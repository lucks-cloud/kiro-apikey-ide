# 更新日志

本文件记录 KIRO-APIKEY-IDE 的版本更新内容。

## v1.0.13

**🔓 开源透明**
- **移除代码混淆**：彻底去除 webview 与扩展主进程的 `javascript-obfuscator` 加密混淆逻辑，打进 vsix 的均为可读源码，便于审阅与二次开发。

**🚀 工程优化**
- **自动化发布**：新增 GitHub Actions 发布流程，推送 `v*` 标签即自动构建并将 `.vsix` 上传到对应 Release，发版更省心。
- **依赖精简**：随混淆移除，清理了 `javascript-obfuscator`、`chalk`、`cli-table3`、`dotenv` 等仅供混淆使用的开发依赖，构建更轻快。

## v1.0.12

**✨ 新增功能**
- **当前使用标识**：网关开启时，正在使用的密钥前会显示绿色「当前」标签，一眼定位在用的 key。
- **单个密钥同步**：右键密钥新增「同步账户信息」，可单独刷新某个密钥的账户信息，无需触发全量同步。
- **版本号徽标**：面板标题旁新增版本号徽标，直观显示当前插件版本。

**💡 体验优化**
- **自动刷新默认开启**：首次使用即自动同步账户订阅类型与积分用量，无需手动开启。
- **同步状态更清晰**：各密钥单独同步时显示独立的加载图标；「立即同步」按钮进行中保持蓝色、仅禁用点击，不再灰显。
- **测试结果更完整**：连通性测试结果新增积分用量（已用 / 总额），并提示「测试通过仅代表 Key 有效，能否实际对话请开启网关并切换到该 Key 后实测」。
- **设置入口更明显**：右上角设置按钮改为带「设置」文字的圆角按钮。
- **常见问题补充**：新增「如何判断是否连接成功」「测试有效但无法对话」「需要人工服务」等 FAQ。
- **重载提示更明确**：需要重载窗口的弹窗移除「稍后」按钮，避免忘记重载导致改动未生效。

**🧹 精简优化**
- **移除「恢复官方服务」按钮**：关闭「网关开关」即等价切回 Kiro 官方服务，去掉冗余入口，操作更直观（相关命令与逻辑一并清理）。

## v1.0.11

**✨ 新增功能**
- **面板日志抽屉**：在「恢复官方服务」后新增「日志」按钮，可直接查看、刷新和复制最近一小时的运行日志，无需再切换到输出控制台。
- **代理链路标记**：KRS / CPS 首次真正收到 kiro-agent 请求时输出醒目标记，可快速区分「未走代理」和「代理已通但上游报错」。

**🔍 诊断增强**
- 扩展激活时打印当前 Kiro 产品版本、底层编辑器版本、构建提交、操作系统架构、检测到的 Kiro / CodeWhisperer 核心扩展版本及本插件版本。

**💡 体验优化**
- 运行日志时间戳由完整 ISO 时间改为本地「时:分:秒」，更简洁易读。
- 账户类型与积分改为按密钥持久缓存：从其他侧边栏切回本扩展不再每次重新请求，仅当距上次同步超过 1 分钟才刷新。
- 刷新账户信息时保留已显示的类型与积分，拿到新结果直接替换，不再短暂清空；同步失败也不会覆盖已有的有效数据。
- 账户信息请求顺序改为从列表最上面的密钥开始。
- 「立即同步」按钮在同步进行中显示加载态并禁用，防止重复点击。
- API Key 列表改为固定高度（约 10 行）内部滚动，密钥很多时不再无限撑高整个面板。
- 操作按钮区（重新加载窗口 / 恢复官方服务 / 日志）与底部按钮在窄侧边栏下自动换行，不再被截断。
- 修正 Kiro 版本识别：不再把底层编辑器版本（如 `1.107.1`）误报为 Kiro 产品版本。
- macOS 优先读取应用包 `Info.plist` 中的 `CFBundleShortVersionString`（如截图中的 `0.12.333`），Windows 增加可执行文件 ProductVersion 回退。

**🛡️ 稳定性优化**
- 端点写入后增加活动 Profile 磁盘读回和有效配置校验，不再把单纯文件写入误报为生效。
- 动态包含当前 Region，并完整校验所有 Region 与端口，修复部分区域被遗漏后静默绕过代理的问题。
- 等待 KRS / CPS 健康检查通过后再覆盖端点，消除固定等待时间导致的启动竞态。
- 修改代理端口时自动重建监听器，并检测工作区设置对用户设置的覆盖冲突。
- fallback 写入前自动备份 settings.json，写入失败时记录准确配置路径和失败阶段。

**🐛 问题修复**
- 修复配置数组中只有任意一项指向 localhost 就被错误判定为完整覆盖的问题。
- 修复端口变化后配置指向新端口、代理却仍监听旧端口的问题。

## v1.0.10

**✨ 新增功能**
- **批量删除**：在「添加」菜单中新增「批量删除」，进入选择模式后默认全选（支持全选 / 反选），点击「删除」并二次确认后一次性删除。为避免误伤，当前正在使用的密钥（网关开启时）会被锁定，不可勾选删除。

**🐛 问题修复**
- 修复删除密钥（含批量删除）后，账户积分统计未同步更新的问题：现在会按密钥身份重新对齐数据并剔除已删除密钥，「已用积分 / 总积分」实时准确。
- 修复了一些已知问题，提升稳定性。

## v1.0.9

**💡 体验优化**
- **当前密钥置顶**：正在使用的 API Key 会固定显示在列表最前面，快速定位。
- **新密钥靠前**：列表按最新添加在前排序，新加入的密钥出现在前面而非末尾。
- **同步更顺滑**：自动刷新账户信息时不再显示「同步中…」，直接保留旧数据并在拉取到新结果后无感刷新。
- **同步失败更友好**：账户信息同步失败的密钥不再显示红色「同步失败」，改为显示「未知身份」标签与 `0/0` 积分，展示更统一。

**🛡️ 稳定性优化**
- **分批同步账户信息**：多密钥同步改为每批 5 个并发、逐批依次请求，兼顾效率与稳定，避免大量密钥同时触发上百个请求造成压力。

**🐛 问题修复**
- 修复了一些已知问题，提升稳定性。

## v1.0.7

**✨ 新增功能**
- **批量导出**：密钥列表新增「批量导出」，可勾选（默认全选、支持全选 / 反选）后导出为 txt 文件，并可自选保存位置。

**💡 体验优化**
- **优化 API Key 连接效率**：请求始终精准使用你选中的密钥，连接更稳定高效。

**🐛 问题修复**
- 修复了一些已知问题，提升稳定性。

## v1.0.6

**✨ 新增功能**
- **快捷搜索 API Key**：密钥列表新增搜索框，可快速筛选定位目标密钥，多密钥管理更方便。
- **自动刷新账户信息**：开启「自动刷新」后，会自动同步每个密钥的账户信息，在列表中直接展示**订阅类型**与**积分用量（已用 / 总额）**，并显示最近同步时间；新添加密钥时也会立即拉取其账户信息。

**🛡️ 安全与交互优化**
- **删除使用中密钥二次确认**：当删除的是「当前正在使用且网关已开启」的密钥时，会弹窗提示该密钥正在使用；确认后自动同步关闭网关，并引导重新加载窗口，期间以加载动画显示进度，避免误删导致服务异常。

**💡 体验优化**
- 优化多处交互细节与提示，整体使用体验更顺畅。

**🐛 问题修复**
- 修复了一些已知问题，提升稳定性。

## v1.0.5

**✨ 核心功能**
- **API Key 直连**：用 `ksk_` 密钥直连 Kiro 官方网关，自带对话即走你的 key。
- **多密钥管理**：可添加多个密钥，请求按顺序自动故障转移（失败切换下一个）。
- **批量导入**：一行一个，一次性导入多个密钥，自动去重与校验。
- **连通性测试**：一键检测密钥可用性，展示订阅信息与可用模型列表。
- **网关总开关**：一键开启 / 关闭接管，关闭后无缝恢复 Kiro 官方服务。
- **地区配置**：支持切换密钥所属 region（默认 `us-east-1`）。
- **一键复制 / 删除**：右键复制完整密钥，快捷删除。
